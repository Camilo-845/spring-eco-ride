server:
  port: ${PASSAGER_SERVICE_PORT}

spring:
  application:
    name: passager-service

  main:
    web-application-type: reactive

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
